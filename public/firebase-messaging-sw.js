/* Placeholder Firebase Messaging Service Worker.
   This prevents 404s when the app requests /firebase-messaging-sw.js.
   Replace with your actual Firebase messaging logic if needed. */

self.addEventListener("install", () => {
  // no-op
});

self.addEventListener("push", () => {
  // no-op
});


